# Project Rules for Career Copilot Workspace

## Git Branch Management Rules
- Whenever you proceed to execute an approved implementation plan, always create a new feature branch from the `develop` branch.
- Once the feature files are accepted and verified, push the feature branch to the remote git repository.
- Finally, merge the feature branch into the `develop` branch.
